# Enerlyze — environment configuration
#
# The app uses exactly ONE environment variable: SECRET.
# It holds the AES-encrypted Firebase config (apiKey, authDomain, projectId, ...).
#
# Generate its value with:  npm run encrypt-env -- firebase-config.json
# Then paste the produced line here (overwriting the placeholder below).

SECRET=PASTE_ENCRYPTED_STRING_HERE
